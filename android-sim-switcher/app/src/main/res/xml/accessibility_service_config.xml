<?xml version="1.0" encoding="utf-8"?>
<!--
  We listen to window state/content changes so we can read the Settings node
  tree and click the right SIM toggle. canRetrieveWindowContent + the click
  action let us drive the UI; we scope to the system Settings package where
  possible to minimise what the service observes.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:description="@string/accessibility_description"
    android:notificationTimeout="100"
    android:packageNames="com.android.settings" />
